Review the Student 1 feature architecture: user profile management (profile CRUD and
resume storage, applicant self-service).

This feature owns its own frontend (student-1-frontend), backend/API
(student-1-backend), and database (student-1-db) tiers. It reuses the shared auth
service (shared-api/shared-db) for identity (users and sessions) via an HttpOnly
session_token cookie, and never stores first/last name or credentials in its own
profiles table.

Resumes (PDF only, 5MB max) are stored as a BLOB in student-1-db, owned entirely
by this feature; resume-level routes live at /resume/{resumeId} rather than
nested under /profile/{id}/resume/{resumeId}. The backend renders HTML
fragments for an HTMX frontend (no JSON API), matching student-2/3's pattern.

The AI resume-autofill capability (suggest professional title/summary/interests from
an uploaded resume, with review/accept/discard via per-field Apply buttons) is
implemented in this release. It is a self-contained AI-Mode integration owned by
this feature (routes/ai_mode.py, services/llm_client.py, prompts/), calling Ollama
directly (llama3.1:8b) the same way student-2/3/4/5 each own their own AI-Mode --
it does not depend on the shared ai-services/ai-mode placeholder.

Use the evidence below to identify:
- responsibilities and ownership boundaries for this feature's services
- dependencies, coupling, or overlapping responsibilities with shared or other services
- duplicated or dead code that should be removed or abstracted into another service, shared or individual
- architectural deviations from the canonical microservices architecture

Distinguish observed evidence from assumptions.

Provide a concise review and at most one or two practical recommendations.
Reply in at most 40 words.
